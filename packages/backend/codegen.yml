overwrite: true
schema: "http://localhost:5555/graphql"
generates:
  src/__generated__/graphql.tsx:
    config:
      scalars:
        DateTime: "Date"
    plugins:
      - "typescript"
      - "typescript-resolvers"
